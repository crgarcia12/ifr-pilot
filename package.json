{
  "name": "ifr-pilot",
  "version": "0.1.0",
  "private": true,
  "description": "IFR Pilot — a tiny browser flight-sim game",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
